---
name: civil-procedure-interpretation
description: 民事訴訟法條文解釋與教學講義產生器。當使用者提供民事訴訟法條文（含條文全文），自動產生 HTML 格式教學講義，包含條文結構解析、要件分析、學說見解、實務見解、虛構教學案例、申論練習題與模擬解答、速查卡。只要對話中出現「民訴第X條」、「民事訴訟法第X條」、「幫我解釋條文」、「做成講義」、「法條教學」、「條文分析」等關鍵字，或使用者貼上民事訴訟法條文全文時，必須立即啟用此 skill。申論題、模擬解答、法律條文、判決均一律由使用者提供，不自行生成；學說見解可由使用者提供，或由 AI 搜尋整理但須附資料來源供使用者驗證後才寫入；教學案例由 AI 虛構，但須標明為教學用途；學說見解與實務見解若尚無內容，須以空白佔位樣式呈現，不可省略章節。
---

# 民事訴訟法條文解釋 Skill

## 核心原則：防幻覺機制

法律資料高度容易產生幻覺，本 Skill 嚴格遵守以下規則：

| 內容類型 | 處理方式 |
|---|---|
| 目標法條條文 | 使用者提供，原文照錄，不得改字 |
| 關聯條文（如駁回事由涉及的移送、罰鍰等條文）| 主動詢問使用者提供，不自行補充 |
| 判決、案號 | 一律由使用者提供，不自行生成 |
| 學說見解 | 使用者提供，或由 AI 搜尋整理（依 Step 3.6 流程），皆須附資料來源供使用者驗證後才寫入 |
| 申論題與模擬解答 | 一律由使用者提供，不自行編造 |
| 比較法（外國立法例）| 不列入講義，除非使用者主動提供並要求加入 |
| 教學案例 | AI 虛構，但須與條文要件高度對應，並標明「教學用途」 |

**條文顯示規則**：使用者提供之條文，顯示前必須與原文逐字比對，確認無誤後方可呈現。

---

## Step 1：收集條文

使用者提供目標法條後：

1. 原文照錄，不改任何字
2. 掃描條文，**主動分析**並向使用者建議關聯條文，格式如下：
   - **必要**（缺了講不完整）：說明原因
   - **建議加入**（幫助理解）：說明原因
   - **可先跳過**（概念較深、非本條核心）：說明原因
3. 待使用者確認或補充條文後，才進入下一步

**分析原則**：
- 必要條文：本條文義直接依賴或前提條文（如條文內明文援引的其他條號、程序銜接條文）
- 建議條文：有助於理解程序效果或救濟途徑（如對應的抗告／上訴條文、後續制裁或處罰條文）
- 可跳過：概念較深、與本條非直接關聯，非本條理解核心，可視情況跳過

**程序銜接類條文特別注意**：
民事訴訟法條文常有「駁回後如何救濟」「本條與前後條構成完整程序鏈」的特性（例如起訴駁回事由與其後之罰鍰條文、抗告與上訴條文常需併同理解）。若條文明文援引其他條號，須向使用者逐一列出並詢問是否一併取得全文，不可僅憑條號猜測內容。

---

## Step 2：確認講義內容範圍

詢問使用者選擇要包含哪些單元：

- **條文結構解析**（必選，不可略去）
- **要件逐項分析**
- **學說見解**（使用者可自行提供，或由 AI 搜尋整理，依 Step 3.6 流程，皆須附資料來源供使用者驗證後才寫入；若尚未提供且搜尋亦查無結果，先以空白佔位呈現）
- **實務見解**（判決／裁定，依 Step 3.5 流程搜尋驗證；若查無或尚未處理，先以空白佔位呈現）
- **教學案例**（AI虛構，標明教學用途）
- **申論練習題與模擬解答**：有三種模式，詢問使用者選擇：
  1. **使用者提供**題目與解答，AI 排版呈現
  2. **AI 自行出題**（須使用者明確授權）
  3. **僅放標題佔位**，題目之後再補入（附來源標示區塊，見 Step 3 說明）
- **速查卡**（記憶口訣，建議保留）

---

## Step 3：產生 HTML 講義

### 產生前置動作：重新讀取最新 SKILL.md

在動手產生 HTML 之前，**一律先以 Contents API `GET` 該 repo 的 `SKILL.md`**，取得 GitHub 上最新版本內容，而非依賴本機掛載或對話記憶中可能過時的規則版本。

- 目的：避免規則已寫入 GitHub（如封面版次／日期／訪客計數器規範）但因讀取到舊版而遺漏執行
- 若發現本機掛載版本與 GitHub 版本有落差，以 **GitHub 版本為準**，並告知使用者發現落差
- 此動作僅為讀取確認，不涉及修改，不需另外詢問使用者

### 固定章節順序

1. 封面（條文名稱、課程資訊、目錄）
2. 條文原文（逐項逐款標示，原文照錄）
3. 條文結構（駁回事由／要件架構總覽表，或程序流程總覽表）
4. 各項 / 各款逐項解析
5. 學說見解（使用者提供內容前，以空白佔位呈現，見下方「空白章節佔位規範」）
6. 實務見解（依 Step 3.5 流程搜尋驗證；查無或尚未處理前，以空白佔位呈現）
7. 教學案例（每案例含：事實、爭點、分析、結論）
8. 申論練習題與模擬解答（若使用者有提供）
9. 速查卡（附記憶口訣）

### 空白章節佔位規範

「學說見解」「實務見解」若尚無內容（使用者未提供、或搜尋查無結果），不得省略該章節，須以佔位樣式呈現：
- 使用虛線框（`.placeholder`），不得留白消失整個章節
- 佔位框內須包含：一個簡短圖示／符號、一行說明文字（例如「尚未加入學說見解內容」）、一個「待補充」徽章
- 側邊導覽選單／手機導覽中，對應該章節的連結需以較淡顏色標示為未完成狀態（不同於已完成章節），但連結本身仍可點擊跳轉

### 申論題來源標示規則

若使用者提供題目來源（教師姓名、學校、課程、原始連結等），須在申論題區塊下方加入來源標示區塊，格式如下：

- 📂 **題目來源**｜教師姓名，學校系所，課程／考試名稱
- 🔗 原始檔案連結（可點擊，`target="_blank"`）

若使用者選擇「僅放標題佔位」，來源標示區塊仍應同步加入（若已知來源）。

### 答題格式規範（申論題）

三段論法須**內化於論述**，不得明文標出「大前提／小前提／結論」字樣。
每段結構為：
> 引用法條規定 → 涵攝具體事實 → 得出結論

**程序駁回／救濟類題目**，論述順序必須依序處理：
1. 先判斷屬「裁定駁回」或「判決駁回」事由（性質定性）
2. 再判斷是否屬可補正事由，法院是否已依法定期先命補正
3. 最後判斷應循抗告或上訴之救濟途徑

不可跳過性質定性直接論救濟方式，否則論述邏輯不完整。

### 程序要件涵攝順序

民事訴訟程序類條文常以「款」列舉多項要件，答題時應**逐款檢視**，不可僅挑選其中一款作答；若題目事實同時該當數款，應併列說明，並指出何款為主要駁回理由。

### HTML 格式規範（法典卷宗設計系統）

- `<head>` 須含 viewport meta tag：`<meta name="viewport" content="width=device-width, initial-scale=1.0">`，並搭配 responsive media query，確保手機版正常顯示
- 使用 Google Fonts（Noto Serif TC 做標題／條號、Noto Sans TC 做內文）

**色彩系統**：
| 用途 | 色票 | 說明 |
|---|---|---|
| 墨藍（主文字／深色背景） | `#16233b` | 標題、速查卡背景、封面印章邊框對比色 |
| 靛藍（次要強調） | `#2c4a76` | 條文框左邊線、案例卡 header、小標題左框 |
| 米宣紙底色 | `#f3ede0`（外層）/ `#fbf8f1`（內容區）/ `#fffdf8`（卡片） | 取代死白背景，全站底色 |
| 印璽紅（重點強調，節制使用） | `#9c2b3a` | 封面印章、否定結論（駁回／不得請求）、待完成章節提示色 |
| 黃銅色（分隔線／記憶口訣） | `#a9832f` | 小標題左框、eyebrow 文字、速查卡口訣底色 |
| 邊框灰米 | `#ddd3bf` | 表格框線、卡片邊框、分隔線 |

**版面結構（桌機／手機）**：
- 桌機：左側 240px sticky 側邊導覽（章節清單，一～九或視章節數調整），右側主內容區，主內容區有淡淡的左側陰影分隔
- 手機（≤980px）：側邊導覽隱藏，改為頂部 sticky 膠囊型橫向導覽（可橫向捲動）
- 導覽需用 `IntersectionObserver` 做 scroll spy：捲動時自動反白目前所在章節連結，點擊連結需平滑捲動（`scrollIntoView({behavior:'smooth'})`）至對應區塊
- 未完成／佔位章節（見「空白章節佔位規範」）在導覽選單中需以較淡顏色（如 `--ink-45`）區隔，但仍可點擊

**封面**：
- 頂部 eyebrow 小標籤（編／章／節資訊），主標題用 Noto Serif TC 900 字重
- 右上角放置一枚旋轉的印章框（`.seal-mark`）：印璽紅邊框、直書關鍵詞（如「駁回」「補正」），營造卷宗印章感

**條文框**：
- 每個條文框（`.article-box`）右下角須放置半透明條號浮水印（大數字，如「249」），字級明顯大於內文、顏色極淡（opacity ≈ 0.06），呼應法典邊注編號的視覺傳統
- 左邊線用靛藍色 3px

**表格**：深墨藍表頭（米宣紙文字）、奇偶列交替底色（`#fffdf8` / `#f6f1e6`）

**結論標示**：
- 肯定結論（合法、應受理等）：加粗深墨藍，並搭配黃銅色底線強調（`linear-gradient` 局部底色）
- 否定結論（駁回、不合法、不得補正等）：加粗印璽紅

**案例卡／申論題卡**：白宣紙底、柔和陰影分層（`box-shadow` 雙層：貼合陰影＋擴散陰影），header 用靛藍（一般案例）或墨藍（申論題／考題）區分性質

**速查卡**：深墨藍背景，右下角放置大型半透明字（如「速」）做裝飾浮水印；記憶口訣區塊用印璽紅底色呈現

**空白章節佔位樣式**（`.placeholder`）：斜紋虛線框（低飽和度黃銅色紋理），置中呈現圖示＋說明文字＋「待補充」膠囊徽章，不得整節留白消失

### 程序流程圖（適用於多階段程序條文）

當條文涉及**多階段程序**（如：起訴 → 審查 → 可補正命補正 → 逾期未補正 → 裁判駁回 → 抗告／上訴），須在條文結構解析之後，插入 SVG 流程圖（`.flow-wrap`），輔助釐清程序脈絡：

**觸發條件**：條文本身即為程序流程（如審查、駁回、救濟類條文）。單純定義性條文（如名詞定義）不需要畫圖。

**版面規則**：
- 每個程序階段一個方框（`rx="6"`，白宣紙底 `#fffdf8`，靛藍框線 `#2c4a76`），框內含階段名稱（14–15px, Noto Serif TC 700）與一行簡短說明（12px, `#5b6472`）
- 導致「駁回」「不得補正」等不利結果的分支節點，框線改用印璽紅 `#9c2b3a`，並在框內加一行印璽紅色小字標出結果（如「裁定駁回」）
- 階段間用箭頭連接：一般流程用靛藍實線箭頭；「可補正／不可補正」等條件分岔，用黃銅色虛線（`stroke-dasharray="5 5"`）標示分支條件文字（4–8字，如「逾期未補正」）
- 版面配置：由左至右或由上至下依程序時間軸排列，分岔節點左右並列
- 圖下方加一行提示文字（`.flow-note`，`ink-45` 淡色置中），複述紅字關鍵結果，避免讀者掃過圖表漏看關鍵
- viewBox 建議 `0 0 640 340`，字型統一用 Noto Serif TC（標題）／Noto Sans TC（說明文字），不使用外部圖示套件，純手繪 SVG 線條與方框

### 人物關係圖（多方當事人案例／申論題適用）

當案例或申論題涉及**三方以上當事人**（含法院、原告、被告、訴訟代理人等關係），須在事實描述段落之後、「試問」之前，插入 SVG 人物關係圖（`.relation-wrap`），版面規則、觸發條件比照上述程序流程圖的視覺語言（方框、箭頭、印璽紅標爭點），僅將節點內容改為當事人角色。

### 版次與訪客計數器（封面固定呈現）

每份講義封面（副標題下方）須加入一行 meta 資訊列（`.cover-meta`），包含：

1. **版次徽章**（`.ver-badge`）：格式 `vX.Y`，從 `v1.0` 起算；日後該講義**任何內容異動**（新增章節、修正條文解析、補充申論題、更新見解等）一律將小數點後版次 +1（如 v1.0 → v1.1 → v1.2）；若是大規模改版（如整體視覺重製、章節結構調整）才將整數位 +1（如 v1.x → v2.0）。版次判斷若不確定屬於小改版或大改版，先詢問使用者
2. **建立／最後更新日期**（`.meta-date`）：YYYY-MM-DD 格式，每次版次更動時同步更新
3. **訪客計數器徽章**：使用 `https://visitor-badge.laobi.icu/badge?page_id=mjib007.civil-procedure.{條號slug}`，`{條號slug}` 採條號組合（例如民訴249條講義用 `249`；跨條號講義如249-249-1用 `249-249-1`），不同講義各自獨立計數，不共用 page_id

三者由靛藍細框（版次徽章用印璽紅細框）+ 淡色文字組成，置於封面副標題正下方，不佔用過多版面高度。

---

## Step 3.5：實務見解搜尋與驗證

當講義涉及實務見解時，依序執行以下流程：

### 搜尋順序
1. 先以關鍵字搜尋網路，確認判決／裁定是否有被引用或討論
2. 同步搜尋是否有大法官解釋或憲法法庭判決涉及本條
3. 整理搜尋到的所有判決／裁定，列表呈現後請使用者確認再寫入講義

### 資料來源處理原則

| 判決類型 | 來源處理方式 |
|---|---|
| 大法官解釋 | 提供 cons.judicial.gov.tw 直接連結 |
| 憲法法庭判決 | 提供 cons.judicial.gov.tw 直接連結 |
| 最高法院判決／裁定（近年） | 提供 judgment.judicial.gov.tw 查詢路徑 |
| 最高法院判例（舊字號） | 提供 law.moj.gov.tw 判例查詢路徑，並向使用者說明「判例」制度已於民國108年法院組織法修正後不再選編，舊判例現僅作為裁判先例參考，不具過去之拘束力 |
| 下級審判決／裁定 | 提供 judgment.judicial.gov.tw 查詢路徑 |

### 無法直接連結時的標準說明
若司法院系統不允許外部直接抓取連結，應告知使用者：
「本裁判可至司法院裁判書查詢系統（https://judgment.judicial.gov.tw/）輸入案號【XX年，XX字，XXXX號】自行查閱全文。」

### 大法官解釋查無時的標準說明
若查無相關大法官解釋或憲法法庭判決，應明確告知：
「本條（§XXX）目前查無大法官解釋或憲法法庭判決，理由為：本條屬［程序性規範／技術性規範］，未有人以此聲請違憲審查。」

### 防幻覺提醒
- 判決字號、年度、內容均不得自行生成，必須來自搜尋結果或使用者提供
- 搜尋後若無法確認全文，僅列案號與查詢路徑，不得自行描述判決內容
- 引用內容須以使用者提供或搜尋已確認者為限
- 若搜尋結果來自非官方網站之節錄轉引，須向使用者明確標註原始來源網址，並提醒尚未經司法院原文核實，建議使用者或 AI 進一步查證後再正式寫入講義

---

## Step 3.6：學說見解搜尋與驗證

當講義涉及學說見解，且使用者未自行提供時，依序執行以下流程：

### 搜尋順序
1. 以關鍵字搜尋學者專書、教科書、期刊論文、法律資料庫（如月旦法學、元照法律學院）中對該條文的見解
2. 亦可搜尋考用書摘（如公職王、保成、新保成等）中整理之學說見解，作為輔助來源
3. 整理搜尋到的所有學說見解，列表呈現後請使用者確認再寫入講義

### 列表格式
每筆學說見解須包含：
- **學者姓名**
- **見解摘要**（濃縮改寫，不逐字照抄原文，比照全站著作權處理原則）
- **出處**（書名／期刊名稱／版次／頁碼，或文章連結）

### 防幻覺提醒
- 學者姓名、見解內容、出處均不得憑記憶生成，須有實際搜尋結果佐證
- 找不到明確來源者不列入，不得用「通說」「多數說」等字眼含糊帶過而不附來源
- 若搜尋結果來自非官方網站之節錄轉引，須向使用者標註原始來源網址，並提醒尚未經原始文獻核實
- 若搜尋後仍查無相關學說見解，比照 Step 3 之「空白章節佔位規範」處理，不得省略該章節

---

## Step 4：使用者審閱修正

使用者審閱後若指出：
- **錯字**：逐一修正，重新輸出
- **內容刪除**：只刪指定部分，其餘不動
- **章節新增**：確認新增內容來源（使用者提供 or AI虛構），再行加入
- **條文有誤**：以使用者提供版本為準，不辯解

### 修正輸出原則

| 修改範圍 | 處理方式 |
|---|---|
| 單一區塊或局部文字 | 用 `str_replace` 局部修改，不重新輸出整份 |
| 多處分散修改或大幅重構 | 整份 HTML 重新輸出 |

**優先使用 `str_replace`**，避免對小改動重新輸出整份講義造成浪費。

---

## Step 5.5：互聯檢查與相關講義連結

執行時機：Step 4 使用者確認講義內容無誤後、Step 5 實際推送之前。

目的：讓每一份講義不再是孤立的頁面，而是能跟其他已存在的講義互相連結，形成一張知識網，而不是一疊各自獨立的檔案。

### 執行流程

1. 讀取總覽頁（`index.html`）目前已收錄的講義清單，取得每份既有講義各自涵蓋的條號範圍
2. 比對本次新講義的：
   - 主要條文條號
   - Step 1 分析出的關聯條文（必要／建議兩類）
   - 講義內文中提及、但屬於其他既有講義範圍的條號
3. 若發現重疊：在新講義**文末、速查卡之後**，新增「相關主題」區塊（`.link-note`，虛線框樣式），列出重疊講義的標題、涵蓋條號、超連結
4. 若未發現重疊：於對話中告知使用者「本次無重疊，略過 Step 5.5」，不需在講義內加入任何區塊
5. **雙向補連**：如果舊講義當初提到某條文時，該條文還沒有獨立講義（只能用文字帶過），現在這條文有了新講義，要主動詢問使用者：「舊講義《○○○》裡提到的第○條，現在已經有獨立講義了，是否要回頭幫舊講義補上這個連結？」不自動修改舊檔案，需使用者確認
6. 連結格式：一律用相對路徑（例如 `../講義/民訴249-1條-濫訴制裁.html`），不寫死網域，方便之後搬遷或本機測試
7. 防幻覺原則：找不到對應講義就不連結，只維持文字提及，絕不虛構連結網址

---

## Step 5：自動推送至 GitHub（選擇性）

Step 4 使用者確認講義內容無誤後，詢問使用者：
「講義已確認完成，是否要推送到 GitHub？如果需要，請提供您的 GitHub Personal Access Token（fine-grained，Contents 權限需為 Read and write）與目標 repo 名稱（預設為 `bigwolfQQ/civil-procedure`）。」

### 推送前檢查
1. 確認使用者提供的 token 格式為 `github_pat_` 或 `ghp_` 開頭
2. 確認目標 repo 名稱格式為 `擁有者/repo名稱`
3. 若使用者未指定檔案路徑，預設路徑為 `講義/民訴{條號}條-{主題}.html`（例如 `講義/民訴249條-起訴之駁回.html`）

### 推送方式
使用 GitHub Contents API（`PUT /repos/{owner}/{repo}/contents/{path}`）：
1. 先以 `GET` 確認該路徑檔案是否已存在（取得 `sha`，若為新檔案則無需此步驟）
2. 將講義 HTML 內容轉為 base64
3. 以 `PUT` 送出，`message` 欄位固定格式：「新增／更新講義：條號＋條文名稱」
4. Commit 到使用者指定分支，若未指定則使用預設分支（通常為 `main`）

### 安全原則
- Token 僅用於當次推送操作，**不得**將 token 寫入任何檔案內容、commit 訊息、或 HTML 講義本身
- 推送完成後，不主動重複顯示 token
- 若推送失敗（權限不足、repo 不存在、token 格式錯誤等），如實告知失敗原因，並提醒使用者檢查 token 權限設定，不得反覆嘗試或猜測替代方案

### 推送完成後
告知使用者：
- 推送成功的檔案路徑與 commit 訊息
- 該檔案在 GitHub 上的網址（`https://github.com/{owner}/{repo}/blob/{branch}/{path}`）

### 同步更新總覽頁 index.html

講義推送成功後，**同步更新 repo 根目錄的 `index.html`**，將本次講義加入總覽頁的卡片清單。

**分類規則（單一主分類）**：
- 每份講義依「主要條文所在編／章／節（／目）」歸入**一個**分類，不掛多節標籤
- 若講義橫跨多節（如主條文在第一審程序，輔助條文在總則編），一律以**主要條文所在節（或目）**為準，其餘節不顯示、不另建分類
- 分類 key 格式為 `{章}-{節}[-{目}]-{編碼}`，**編碼放在字串最後一段**，章／節／目依序排在編碼之前；若該章無節，則省略節碼；若該編本身無章節劃分，則 key 僅為該編碼本身：

| 編碼 | 對應編 |
|---|---|
| `1` | 第一編 總則 §1–243 |
| `2` | 第二編 第一審程序 §244–436-32 |
| `3` | 第三編 上訴審程序 §437–481 |
| `4` | 第四編 抗告程序 §482–495-1（無章節劃分） |
| `5` | 第五編 再審程序 §496–507（無章節劃分） |
| `5-1` | 第五編之一 第三人撤銷訴訟程序 §507-1–507-5（無章節劃分） |
| `6` | 第六編 督促程序 §508–521（無章節劃分） |
| `7` | 第七編 保全程序 §522–538-4（無章節劃分） |
| `8` | 第八編 公示催告程序 §539–567（無章節劃分） |

範例：
- 當事人能力及訴訟能力（總則編／第二章／第一節）→ `2-1-1`
- 管轄（總則編／第一章／第一節）→ `1-1-1`
- 起訴（第一審程序編／第一章／第一節，第249條所在）→ `1-1-2`
- 人證（第一審程序編／第一章／第三節／第二目）→ `1-3-2-2`
- 簡易訴訟程序（第一審程序編／第三章，無節）→ `3-2`
- 第三審程序（上訴審程序編／第二章，無節）→ `2-3`
- 第三人撤銷訴訟程序（第五編之一，無章節）→ `5-1`（key 本身即為編碼，無章節碼疊加）

**卡片欄位**：僅顯示條號、主題、日期（不含授課對象等其他資訊）。

**執行步驟**：
1. 向使用者確認本份講義的「條號」、「主題」（簡短標題）、「完成日期」、「主分類 key」
2. 以 `GET` 取得目前 `index.html` 的內容與 `sha`
3. 在 `LECTURES` 陣列中新增一筆物件（`articles`、`title`、`date`、`chapterKey`、`file`），僅新增，不覆蓋既有項目
4. 以 `PUT` 推送更新後的 `index.html`，`message` 欄位固定格式：「更新總覽頁：新增《條號＋主題》」
5. 若目標分類 key 不在上表範圍內，先告知使用者目前總覽頁分類架構尚未涵蓋，暫不強行分類

---

## 記憶口訣產生原則

速查卡應包含記憶口訣，口訣須：
- 對應條文核心差異（如裁定駁回 vs 判決駁回、可補正 vs 不可補正）
- 以對仗或押韻方式呈現
- 不超過 20 字
範例（民事訴訟法第249條）：
> 「一項裁定，二項判決；逾期不補，駁回確定」
